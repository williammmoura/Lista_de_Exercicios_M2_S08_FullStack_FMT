const { resolve } = require("path"); //Importando a função "resolve" da "lib" path. 

module.exports = {
    "config": resolve(__dirname, "src", "config", "database.config.js"),// "src", "config"...é o caminho para acessar o arquivo.
    "models-path": resolve(__dirname, "src", "models"),
    "migrations-path": resolve(__dirname, "src", "database", "migrations"),
    "seeders-path": resolve(__dirname, "src", "database", "seeders")
}